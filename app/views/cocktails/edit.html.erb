<div class="container">
  <div class="row">
    <div class="col-12">
      <%= simple_form_for(@cocktail) do |f| %>
        <%= f.input :name %>
        <%= f.submit "Update cocktail", class: "btn btn-success"%>
      <% end %>
      <%= link_to '<i class="fas fa-glass-martini-alt"></i> Back to cocktails'.html_safe, root_path, class: "btn btn-success" %>
    </div>
  </div>

</div>
